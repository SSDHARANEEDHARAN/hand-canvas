import { GestureScene } from "@/components/GestureScene";

const Index = () => {
  return (
    <main>
      <h1 className="sr-only">Gesture-controlled 3D particle playground</h1>
      <GestureScene />
    </main>
  );
};

export default Index;
